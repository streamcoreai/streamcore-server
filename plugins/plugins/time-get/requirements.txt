# time-get only uses the one-argument on_execute, so 0.1 still works. 0.2 also
# answers the server's ready pass instead of logging an error at startup.
#
# To run against a local checkout of the SDK instead:
#   PLUGIN_SDK_DIR=../plugin-sdk go run .
streamcore-plugin>=0.2.0
