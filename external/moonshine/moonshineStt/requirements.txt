# Moonshine STT sidecar.
#
# moonshine-voice ships the native runtime and pulls numpy in with it; the
# model itself is downloaded on first run and cached.
moonshine-voice>=0.1.5
websockets>=12.0
numpy>=1.24.0
